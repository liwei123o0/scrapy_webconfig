# Automatically created by: scrapyd-deploy

from setuptools import setup, find_packages

setup(
    name         = 'project',
    version      = 'V1.0',
    packages     = find_packages(),
    entry_points = {'scrapy': ['settings = xbzxproject.settings']},
)
